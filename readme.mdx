p This project was made with HTML and CSS web development stack.
p You can use different Text Editors such as VSCode to run these files.
p Testing was done using Firefox browser and images are in jpg file extensions.
p Files are stored in different directories.
p HTML is found in the html folder with the file name called index.html.
p CSS is found in the css folder with the file name called styles.css.
